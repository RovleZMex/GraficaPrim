//
// Created by Santiago Robles on 16/05/23.
//

#include "GraficaPrim.h"


Grafica Prim::HacerAlgoritmo(const Grafica& g) {
/*
    std::vector<Grafica::Nodo> visitados;
    Grafica::Nodo temp(g.ObtenerPrimerNodo());
    visitados.push_back(temp);

    while(visitados.size() != g.ObtenerNumNodos()){
        for(Grafica::Nodo n : visitados){
            if()
        }
    }
*/

    return Grafica();
}
